# Text Morph AI Processing

A minimal project for text paraphrasing and summarization.

---

## 📂 Files

- `paraphraser.py`
- `summarizer.py`

---

## ⚙️ Setup

1. Install dependencies:

```bash
pip install -r requirements.txt
Run the scripts:
python paraphraser.py
python summarizer.py
🚀 Usage
paraphraser.py → paraphrase input text
summarizer.py → summarize input text
🔗 GitHub Push (for reference)
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin <your-repo-url>
git push -u origin main

---

## 🔥 Extra (optional but impressive 😎)
Agar thoda aur pro banana hai, ye add kar:

```md
## 🧠 Technologies Used

- Python
- Transformers (Hugging Face)
- Streamlit
- PyTorch
💯 Result

Tera README:

clean lagega ✅
professional lagega ✅
viva me impress karega ✅

Agar chahe to main iska frontend + backend wala advanced README bhi bana dungi (next level project look 🔥)

i need for app.py

Samajh gayi 👍 — tu specifically app.py (Streamlit app) ke liye README chahti hai.

Ye thoda different hota hai kyunki isme UI run hota hai (browser me), simple .py script jaisa nahi.

✅ Copy this README for app.py
# Text Morph AI Web App

A Streamlit-based web application for text paraphrasing and summarization using NLP models.

---

## 📂 Files

- `frontend/app.py` → Streamlit web interface  
- `backend/` → FastAPI backend (if applicable)  
- `requirements.txt` → project dependencies  

---

## ⚙️ Setup

1. Clone the repository:

```bash
git clone <your-repo-url>
cd <your-repo-folder>
Create virtual environment (optional but recommended):
python -m venv venv
venv\Scripts\activate
Install dependencies:
pip install -r requirements.txt
🚀 Run the App
streamlit run frontend/app.py

👉 If not working, use:

python -m streamlit run frontend/app.py
🌐 Output
The app will open in your browser
Default URL: http://localhost:8501
🧠 Features
✨ Text Paraphrasing
📄 Text Summarization
🎯 Multiple difficulty levels (if implemented)
⚡ Fast UI using Streamlit
🛠️ Technologies Used
Python
Streamlit
Transformers (Hugging Face)
PyTorch
🔗 GitHub Push
git add .
git commit -m "Added Streamlit app"
git push

---

## 💡 Important (samajh le)
Difference:

| File type | Run command |
|----------|------------|
| normal `.py` | `python file.py` |
| Streamlit app | `streamlit run file.py` |

---

## 🔥 Pro tip (viva me bolna)
> This project uses Streamlit to create an interactive web interface for NLP tasks like paraphrasing and summarization.

---

Agar tu chahe to main tera **app.py ka UI bhi upgrade karwa sakti hoon (buttons, styling, etc.)** 😎
