
# Text Morph AI Web App

A Streamlit-based web application for text paraphrasing and summarization using NLP models.

---

## 📂 Files

- `frontend/app.py` → Streamlit web interface  
- `backend/` → FastAPI backend (if applicable)  
- `requirements.txt` → project dependencies  

---

## ⚙️ Setup

1. Clone the repository:

```bash
git clone <your-repo-url>
cd <your-repo-folder>
Create virtual environment (optional but recommended):
python -m venv venv
venv\Scripts\activate
Install dependencies:
pip install -r requirements.txt
🚀 Run the App
streamlit run frontend/app.py

👉 If not working, use:

python -m streamlit run frontend/app.py
🌐 Output
The app will open in your browser
Default URL: http://localhost:8501
🧠 Features
✨ Text Paraphrasing
📄 Text Summarization
🎯 Multiple difficulty levels (if implemented)
⚡ Fast UI using Streamlit
🛠️ Technologies Used
Python
Streamlit
Transformers (Hugging Face)
PyTorch
🔗 GitHub Push
git add .
git commit -m "Added Streamlit app"
git push

---

## 💡 Important (samajh le)
Difference:

| File type | Run command |
|----------|------------|
| normal `.py` | `python file.py` |
| Streamlit app | `streamlit run file.py` |

---

## 🔥 Pro tip (viva me bolna)
> This project uses Streamlit to create an interactive web interface for NLP tasks like paraphrasing and summarization.

---
