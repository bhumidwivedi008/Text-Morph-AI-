Text Morph AI Processing
A minimal project for text paraphrasing and summarization.

Files
paraphraser.py
summarizer.py
Setup
Install dependencies (if any). Example:
pip install -r requirements.txt
Run the script(s):
python paraphraser.py
python summarizer.py
Usage
paraphraser.py : paraphrase input text
summarizer.py : summarize input text
GitHub push
git init
git add .
git commit -m "Add initial paraphraser/local summarizer and README"
git branch -M main
git remote add origin <your-git-repo-url>
git push -u origin main
